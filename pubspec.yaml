name: hardware_compatibility_gpt
description: Custom GPT for Hardware Part Compatibility Analysis
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  provider: ^6.0.5
  shared_preferences: ^2.0.18
  flutter_markdown: ^0.6.18
  url_launcher: ^6.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

